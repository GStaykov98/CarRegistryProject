<Window x:Class="CarRegistryProject.PersonProfileWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CarRegistryProject"
        mc:Ignorable="d"
        Title="PersonProfileWindow" Height="520" Width="900">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock x:Name="HeaderText"
                   Grid.Row="0"
                   FontSize="18"
                   FontWeight="Bold"
                   Margin="0,0,0,6"/>

        <!-- Basic info -->
        <TextBlock x:Name="InfoText"
                   Grid.Row="1"
                   FontSize="13"
                   Margin="0,0,0,12"/>

        <Button x:Name="AddTestCarButton"
        Grid.Row="2"
        Content="Add Test Car"
        Width="140"
        Height="28"
        HorizontalAlignment="Left"
        Margin="0,0,0,10"
        Click="AddTestCar_Click"/>

        <!-- Cars table -->
        <DataGrid x:Name="CarsGrid"
                  Grid.Row="3"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  MouseDoubleClick="CarsGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Make" Binding="{Binding Brand}" Width="*"/>
                <DataGridTextColumn Header="Model" Binding="{Binding Model}" Width="*"/>
                <DataGridTextColumn Header="Reg. No" Binding="{Binding RegistrationNumber}" Width="160"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
